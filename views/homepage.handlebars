{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{post.title}}</h3>
    <p>{{post.description}}</p>
  </div>
</div>
{{/each}}
